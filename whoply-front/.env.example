# Whoply — marketing / landing site (:7100 in dev)
# Copy to `.env.local` (or run `node setup.mjs` from the repo root).
NEXT_PUBLIC_API_URL=http://localhost:7000/api   # API base, including /api
NEXT_PUBLIC_APP_URL=http://localhost:7200       # where "Login / Get started" links to
# Production: https://api.yourdomain.com/api  and  https://app.yourdomain.com
